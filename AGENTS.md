# 盘迹 AGENTS（Agent 工作规则）

所有参与角色在开始任何任务前必须完整阅读本文件。**仓库文件是唯一事实来源，聊天记忆不作为依据。**

## 1. 项目与阶段

- 盘迹：仅面向中国大陆市场的 iOS 文玩盘玩记录社区。核心：创建玩物 → 持续记录盘玩变化 → 形成成长时间轴 → 后续分享到社区并与同好交流。
- 当前只开发 M0（工程底座）+ M1（玩物记录核心闭环）。**M1 真机主链（20 步）验收通过前，禁止启动 M2 社区开发。**
- M1 禁止开发：社区、帖子、评论、点赞、关注、消息中心、私信、商城、交易、竞拍、直播、鉴定、AI、会员、等级、积分、勋章、Android、完整 Web 客户端、真实微信登录、真实 Sign in with Apple、真实短信验证码。
- 正式 V1 登录最终为手机号验证码 + 微信 + Apple；M1 统一使用 Dev Login。

## 2. 任务开始前（强制阅读）

1. `/AGENTS.md`（本文档）
2. `docs/PRODUCT_SPEC.md`
3. `docs/ARCHITECTURE.md`
4. `docs/API_CONTRACT.md`
5. `docs/DATABASE_SCHEMA.md`
6. `docs/DESIGN_SYSTEM.md`
7. `docs/TASKS.md`
8. `docs/handoffs/` 中与当前任务相关的交接单

## 3. 裁决权限（变更控制）

只有总控（Lead）有权最终批准：

- V1 范围、API 路径、API 公共字段、数据库公共结构、技术栈、第三方核心依赖、文件总体架构、跨模块协议。

其他角色发现问题只能提交建议，**未经批准不得自行实施**，建议类型：

- `ARCHITECTURE_CHANGE_PROPOSAL`
- `API_CHANGE_PROPOSAL`
- `SCHEMA_CHANGE_PROPOSAL`
- `PRODUCT_CHANGE_PROPOSAL`

## 4. 修改范围与技术红线

- 只修改自己任务所需的文件；不做与任务无关的大规模重构；不擅自修改他人已确认的公共契约。
- 禁止引入：微服务、Redis、Elasticsearch、消息队列、Kubernetes、AI 服务、GraphQL（除非经明确架构评审批准）。

## 5. 任务与状态

- 任务唯一编号：`M0-xxx` / `M1-xxx`；一次一个角色、一个可独立验收的任务；任务必须可测试、可拒绝、可审核。
- 状态只允许：`BACKLOG` / `READY` / `IN_PROGRESS` / `REVIEW` / `BLOCKED` / `DONE`，不得使用模糊状态。
- 流转：BACKLOG → READY（依赖就绪并派发）→ IN_PROGRESS → REVIEW（提交产出+交接单）→ DONE（总控审核通过）；REVIEW 不通过 → IN_PROGRESS（退回整改）；依赖未满足 → BLOCKED。

## 6. 验证规则

禁止"理论上可以 / 应该没问题 / 看起来完成 / 代码写好即完成"。**DONE 必须有验证证据**，按任务性质至少提供其一并附运行命令与结果：

- 单元测试 / 集成测试 / lint / typecheck / build / 数据库 migration / API 测试 / iOS 构建 / 真机测试

## 7. 交接制度

任务完成后必须写 `docs/handoffs/<TASK-ID>.md`，至少包含 16 项：

1. 任务编号 2. 角色 3. 完成状态 4. 完成内容 5. 修改文件 6. 新增文件 7. API变化 8. 数据库变化 9. UI变化 10. 测试命令 11. 测试结果 12. 构建结果 13. 已知问题 14. 风险 15. 对其他角色影响 16. 建议下一任务

总控收到交接单后：阅读实际 diff/文件（不只相信自述）→ 判断是否需要 QA → 检查是否违反公共规格 → 更新 TASKS.md → 再分配下一任务。

## 8. 产品原则（所有设计必须遵守）

1. 少字段。2. 少层级。3. 少步骤。4. 图片优先。5. 不提前实现未来功能。6. 不为了架构漂亮增加不必要复杂度。7. 创建玩物约 20 秒内完成。8. 一次盘玩记录约 10 秒内完成。9. 玩物详情本质是一件玩物的长期相册日记。10. 所有功能围绕"记录、展示、交流"服务。

## 9. M1 最终验收（真实 iPhone，20 步）

1 启动盘迹 → 2 Dev Login → 3 设置昵称 → 4 进入收藏柜 → 5 查看空状态 → 6 创建"四座楼狮子头" → 7 选择"核桃" → 8 可添加封面 → 9 创建成功 → 10 自动进入详情 → 11 点击记录第一天 → 12 添加照片 → 13 输入"刚到手，先记录一下。" → 14 保存 → 15 时间轴出现记录 → 16 补录过去日期 → 17 时间轴排序正确 → 18 关闭 App → 19 再次打开 → 20 数据仍然正确存在。

只有该主链最终通过，总控才批准进入 M2 社区阶段。
