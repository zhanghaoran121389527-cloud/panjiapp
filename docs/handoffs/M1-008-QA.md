# M1-008 QA 审核交接单（QA）

- 审核对象：M1-008【iOS】创建玩物（创建后自动进详情）
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-25（同日）
- 审核依据：API_CONTRACT v2.3 §3.6/3.10、DESIGN_SYSTEM v1（§6.1/§6.4/§6.5）、docs/design/04-create-item.md、验收清单 v2.1（B08/B09/B10/B12）、TASKS M1-008 验收条件
- 审核方式：实读全部相关实现文件（CreateItemView/CreateItemStore/ItemDetailPlaceholderView + CabinetView 接线 + APIClient/APIDTOs/PanJiTheme 触点）+ 云端 CI 构建证据 + 截图像素核验

## 冒烟项核对（B08/B09/B10/B12，静态级 PASS）

| 编号 | 场景 | 实现（实读） | 结果 |
|---|---|---|---|
| B08 | 照片上传失败/选图取消 | 上传失败 → coverError（08-states B 表文案）显示于封面区下方，可「移除」封面**跳过继续创建**（不阻塞）；选图器取消 → `guard let item else return`，不产生半提交 | PASS |
| B09 | 快速重复点击保存 | `canSubmit` 含 `!submitting && !uploadingCover`；提交中按钮禁用 + 文案「上传封面中…/创建中…」（裁决 A14） | PASS |
| B10 | 名称空/全空格/超 50 | trim 后空 → canSubmit=false；输入 >50 硬截断（onChange prefix(50)） | PASS |
| B12 | 无封面创建 | 封面跳过 → `coverImageUrl=null`（契约空值规则）；卡片首字占位已在 M1-007 复核 | PASS |

## 关键流程核对

| 项 | 实现 | 结果 |
|---|---|---|
| **两段式（重试不重传）** | `coverURL` 缓存：`if url == nil, let data = coverData` 才上传；创建失败重试直接复用已传 URL（08-states D） | PASS |
| 品类 5 芯片 | ChipFlowLayout 流式换行；单选不可取消（线框 04/B4 裁决）；加载失败行内错误+「重试」 | PASS |
| 选填折叠 | 「更多信息」默认收起，0.2s 动画；子类≤50/入手日期（上限=北京时间今天 23:59:59，可清除）/尺寸≤100/备注多行 minHeight 88 ≤500 | PASS |
| 创建后自动进详情 | CabinetView `onCreated`：关 sheet + `showDetail` push `ItemDetailPlaceholderView`（M1-009 替换，导航链路已通） | PASS |
| 回刷收藏柜 | 同回调 `Task { await store.refresh() }`（线框 03 注意事项） | PASS |
| 表单空值规则 | `nilIfBlank`：选填 trim 空一律 null（契约空值规则）；入手日期按北京时间格式化 YYYY-MM-DD（A8/A13） | PASS |
| multipart 上传 | 手写 multipart 字段名 `file` + Bearer；MIME 从文件头推断（jpeg/png/webp/heic-heif 族）；原生 PhotosPicker 单选，HEIC 原样上传（3.10 白名单支持） | PASS |
| 20 秒体验观察项 | 打开即聚焦名称（0.4s 延迟）、5 芯片 1 击、封面可跳过、选填默认折叠、底部常驻「创建」——3 步内完成路径静态成立；**实测计时并入 M1-012** | PASS（静态支撑） |

## 构建证据（云端 CI）

- 19:18 CI 运行（M1-008 代码，Sources=17）：**`** BUILD SUCCEEDED **`** + 模拟器 boot/install/**launch（pid 3225）** + 截图回传 + `STEP4_ALL_OK` ✓。
- 新截图像素核验（1206×2622）：暖米白 90.8% / 木棕 1.5% / 深茶褐 0.3% / surface 4.9% —— 登录页特征，App 正常启动无崩溃 ✓。

## 工程与范围核对

- 越界申报：Network 两处（uploadImage + MIME/扩展名，上传能力归网络层）+ PanJiTheme 一处（PanJiTextField 多行 axis/minHeight，DS §6.4 组件规范）为最小必要触点，交接单已申报，总控初审接受；其余均在 Features/Items 允许范围。无其他越界。
- pbxproj 注册 3 新文件、CreatePlaceholderView 移除（CI 编译佐证）；无 M1 外功能。

## 发现的问题

- 无阻断问题。
- 观察项（交互级，并入 M1-012）：① sheet 关闭与 navigationDestination push 同帧触发（`showCreate=false` + `showDetail=true` 同回调），SwiftUI 同帧状态合并下需真机确认 push 生效；若失效，iOS 可在 M1-009 接线时加 0.3s 延迟；② MIME 推断兜底为 image/jpeg（未知文件头），PhotosPicker 实际来源均覆盖，边缘不影响；③ optionalField 复用同一 FocusState（子类/尺寸），键盘行为无碍，属实现取舍；④ 提交中仍可点「取消」关闭 sheet，后台 Task 会继续执行（创建成功仍会走 onCreated），属边缘竞态，M1-012 观察。

## 最终结论

**PASS**（附 NOT VERIFIED：交互级冒烟与 20 秒计时并入 M1-012；CI 构建+launch 证据已齐）

## 是否允许 M1-008 置 DONE

**YES** —— 建议总控将 M1-008 置 DONE；其后 M1-009（详情+时间轴，替换 ItemDetailPlaceholderView）与 M1-011（编辑/删除，表单可复用）前置满足。

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M1-008 独立复核（B08/B09/B10/B12 + 两段式/芯片/折叠/自动进详情/回刷 + CI 绿 + 截图像素核验）→ PASS |
