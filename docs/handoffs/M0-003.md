# M0-003【iOS】SwiftUI 工程底座 — 交接单

- 任务编号：M0-003（原 T-003，编号迁移见 TASKS v3 变更记录；本单由 `T-003.md` 更名而来）
- 角色：iOS
- 完成状态：BLOCKED（产出与静态证据完整；总控本轮要求补齐 Xcode 构建证据，经环境核查本执行环境为 Windows、无 macOS/Xcode，构建证据无法在此产出，按 AGENTS §5「依赖未满足 → BLOCKED」置位。待总控裁决：由 QA 在 M0-006 于 macOS 执行构建，或提供 macOS 执行机）
- 日期：2026-08-19

## 1. 完成内容

- **目录迁移**：`ios/` → `apps/ios/`（ARCHITECTURE 裁决 B1「统一 apps/<端> 布局」）。工程内全部引用为相对路径，迁移后结构与有效性不变。
- SwiftUI 工程骨架 `apps/ios/PanJi.xcodeproj`（objectVersion 56，Xcode 14/15/16 可打开）+ 共享 scheme（CLI xcodebuild 可用）。
- baseURL 注入链：`Config/Config.xcconfig`（`API_BASE_URL`）→ `Info.plist`（`APIBaseURL` = `$(API_BASE_URL)`）→ `AppConfig.apiBaseURL`；Debug/Release 共用，一处改配置即切换。
- App 壳占位首页 `RootView`（M0 验收用，M1-006 起替换）。
- 暂定主题 token `Color.PanJi.*`（与 DESIGN_SYSTEM v0 表格一致）。
- 占位 AppIcon（1024×1024）、AccentColor（木棕 #8C6D4F）、README、.gitignore。
- 总控裁决落实：B2（仅 iPhone、锁竖屏、最低 iOS 17.0）✓ 已按此配置；B3（Bundle ID 占位 `com.panji.app`）✓。

## 2. 修改文件（本任务内修订，均为自建文件）

- `apps/ios/Config/Config.xcconfig`：前缀注释对齐 `/v1/`（裁决 A9）
- `apps/ios/README.md`：契约引用改 `docs/API_CONTRACT.md`；标题路径改 `apps/ios/`
- 其他既有文件无改动。

## 3. 新增文件（14 个，全部位于 apps/ios/）

```
apps/ios/Config/Config.xcconfig
apps/ios/PanJi.xcodeproj/project.pbxproj
apps/ios/PanJi.xcodeproj/xcshareddata/xcschemes/PanJi.xcscheme
apps/ios/PanJi/App/PanJiApp.swift
apps/ios/PanJi/App/RootView.swift
apps/ios/PanJi/Core/DesignSystem/PanJiTheme.swift
apps/ios/PanJi/Core/Network/AppConfig.swift
apps/ios/PanJi/Resources/Info.plist
apps/ios/PanJi/Resources/Assets.xcassets/Contents.json
apps/ios/PanJi/Resources/Assets.xcassets/AccentColor.colorset/Contents.json
apps/ios/PanJi/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json
apps/ios/PanJi/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png
apps/ios/README.md
apps/ios/.gitignore
```

## 4. 使用 API

无业务接口实现（M0）。约定锁定：`API_BASE_URL` = 主机根地址（不含路径，如 `http://192.168.1.100:3000`）；业务前缀 `/v1/`（裁决 A9）与静态图片 `/uploads/*`（裁决 A3）由网络层在 M1-006 拼接。

## 5. API 变化

无（未新增/未修改任何接口字段）。

## 6. 数据库变化

无。

## 7. UI 行为 / UI 变化

- 仅占位首页：暖米白背景（`#F6F1E7`）+ 木棕「盘」圆标（`#8C6D4F`）+ 深茶褐「盘迹」标题（`#4A3728`）+ 底部次文字显示当前注入的 API 地址。
- 显示 API 地址是为验收「改配置切换 baseURL」提供目视证据；登录/昵称/收藏柜等正式页面属 M1-006 起，本任务不实现。

## 8. 空状态

M0 无业务页面，无空状态设计（空态规范由 M0-004 线框覆盖，实现于 M1-007/M1-009）。

## 9. 异常状态

- `AppConfig.apiBaseURL` 缺失或为空时 `preconditionFailure`——开发期快速暴露 xcconfig 注入链断裂，不静默降级。
- 网络错误/401/超时等正式异常处理属 M1-006，本任务不实现。

## 10. 测试命令（Windows 环境内执行的验证）

```powershell
# 1) plist/scheme XML 解析
[xml](Get-Content -Raw 'apps\ios\PanJi\Resources\Info.plist')
[xml](Get-Content -Raw 'apps\ios\PanJi.xcodeproj\xcshareddata\xcschemes\PanJi.xcscheme')
# 2) 资产目录 JSON 解析（3 个 Contents.json，ConvertFrom-Json）
# 3) pbxproj 结构：括号配平、对象定义唯一性、引用闭合（正则交叉核对）
# 4) scheme BlueprintIdentifier 与 pbxproj target 一致性
# 5) 全部文本文件严格 UTF-8 解码
# 6) AppIcon 生成（System.Drawing）并回读尺寸
```

## 11. 测试结果

| 检查 | 结果 |
|---|---|
| Info.plist / xcscheme XML 解析 | PASS |
| 3 个资产目录 JSON 解析 | PASS |
| pbxproj 括号配平（43/43）、33 个对象 ID 全唯一且 24 位、全部引用有定义 | PASS |
| scheme 指向已定义 target | PASS |
| 全部 Swift/xcconfig/plist/md 严格 UTF-8 | PASS |
| AppIcon 1024×1024 PNG（18,314 字节） | PASS |
| 目录迁移后全量复检（apps/ios/ 结构完整） | PASS |
| 本轮·构建能力核查（xcodebuild / xcrun / sw_vers / xcode-select / Simulator / Docker） | 全部 ABSENT——执行环境为 Windows 10，无 macOS/Xcode |
| 本轮·总控 6 验证点静态映射（注入链路 4 + README 3 + Info.plist·ATS 5 + pbxproj 设置 3 + RootView/scheme 3 + Sources 落盘 2） | PASS 20/20 |

## 12. 构建结果

**Xcode 构建：NOT EXECUTED（环境阻断，不伪造证据）。**

总控本轮正式要求补齐 Xcode 构建证据，已执行环境核查并留存证据：本 Agent 运行于 Windows 10（OSDescription: Microsoft Windows 10.0.26200），`xcodebuild` / `xcrun` / `sw_vers` / `xcode-select` 均不存在，无 iOS Simulator，无 Docker。真实 Xcode 构建与模拟器启动在本环境物理不可行。

**尚未完成 Xcode 构建验证。尚未完成真机验证。**

macOS 执行机上的构建命令 runbook（供 M0-006/QA 或总控提供的 macOS 环境直接复用，执行后把日志回填本单 §12）：

```bash
cd apps/ios
# ① 完整 Build（模拟器目标）
xcodebuild -project PanJi.xcodeproj -scheme PanJi \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  build 2>&1 | tee build-m0-003.log
# ② 核对注入链路：产物 Info.plist 中 APIBaseURL 应等于 http://localhost:3000
plutil -p ~/Library/Developer/Xcode/DerivedData/PanJi-*/Build/Products/Debug-iphonesimulator/PanJi.app/Info.plist | grep APIBaseURL
# ③ 启动 App（模拟器）
xcrun simctl boot "iPhone 15" || true
open -a Simulator
xcrun simctl install booted ~/Library/Developer/Xcode/DerivedData/PanJi-*/Build/Products/Debug-iphonesimulator/PanJi.app
xcrun simctl launch booted com.panji.app
# ④ 目视验证：占位页显示「盘迹」与 API: http://localhost:3000
# ⑤ baseURL 切换验证：改 Config/Config.xcconfig 的 API_BASE_URL 后重新 build，占位页 API 地址随之变化
```

## 13. 未验证项

- Xcode 构建：NOT EXECUTED（本轮环境核查确认执行环境为 Windows、无 xcodebuild；runbook 已存档 §12，待 M0-006 或 macOS 执行机执行）
- 模拟器/真机运行：NOT EXECUTED（同上）
- 局域网真机联调（切换 xcconfig 局域网 IP 后的实际联通性，M1-006 联调时验证）

## 14. 已知问题

- 手写 pbxproj 未经 Xcode 首次打开实测；如遇"update to recommended settings"告警属正常，可忽略或升级。
- 主题色为 DESIGN_SYSTEM v0 暂定值；M0-004 定稿 v1 后由 iOS 在 M1-006 起统一替换（仅 `PanJiTheme.swift` 一处，无扩散）。
- 无单元测试 target（M0 骨架不建，YAGNI；如需在 M1-006 一并加）。
- 真机签名 `DEVELOPMENT_TEAM` 留空，开发者需自选 Team（模拟器免签可跑）。

## 15. 风险

- 若 iOS 开发者首次打开工程发现 pbxproj 无法解析（可能性低，已做引用闭合校验），退回本任务修复即可，不影响其他角色。
- ATS：已开 `NSAllowsLocalNetworking`；若个别网络环境仍拦截局域网 http，备选 Debug 下临时 `NSAllowsArbitraryLoads=true`（当前未启用，改前与 iOS 确认）。

## 16. 对其他角色影响

- QA（M0-006）：需在 macOS 补「iOS 构建证据」；真机联调按 README 改 xcconfig。
- UI（M0-004）：暂定 token 表已入 DESIGN_SYSTEM v0；定稿后 iOS 替换。
- BE：无影响（未调用接口）。
- 总控：B1 迁移已执行（`ios/` 已不存在，`apps/ios/` 就位）；交接单已更名 M0-003.md。

## 17. 后端依赖

- M0-002（health 就绪）后可用于联调冒烟；真实接口依赖自 M1-002 起。

## 18. QA 关注点

- 构建证据需 macOS 环境（M0-006 项 ③）。
- README 契约引用已为 `docs/API_CONTRACT.md`（M0-003 QA 要求 ✓）。
- 真机联调：ATS 例外已开；baseURL 切换方式见 `apps/ios/README.md`。

## 19. 建议下一任务

- iOS：M1-006（网络层 + 登录/昵称），前置 = M0-003（本任务）+ M0-004（UI 线框/定稿 token）+ M1-002（BE）。当前均未 READY，等待总控派发。
- 同时建议总控把 M0-003 置 DONE（迁移已完成）。

## 20. 变更记录

| 时间 | 变更 |
|---|---|
| 2026-08-19 | 交接单自 `docs/handoffs/T-003.md` 更名至 `docs/handoffs/M0-003.md`（编号迁移 Mx-xxx） |
| 2026-08-19 | 执行裁决 B1：`ios/` → `apps/ios/`（README 标题同步） |
| 2026-08-19 | 总控要求补齐 Xcode 构建证据：环境核查（Windows、无 Xcode）+ 6 验证点静态映射 20/20 PASS + macOS runbook 存档；构建 NOT EXECUTED；状态 → BLOCKED |

## 21. 本轮验证补充记录（总控要求：Xcode 构建验证，6 项逐条）

| 总控要求 | 结果 |
|---|---|
| 1. 使用实际 macOS + Xcode 打开工程 | NOT EXECUTABLE：执行环境为 Windows 10，无 macOS/Xcode（证据见 §12） |
| 2. 选择可用 iOS Simulator | NOT EXECUTABLE：无 macOS/Simulator |
| 3. 执行完整 Build | NOT EXECUTED：`xcodebuild` 不存在；可执行命令序列已存档（§12 runbook） |
| 4. 启动 App | NOT EXECUTED：同上 |
| 5a. RootView 正常显示 | 静态 PASS：PanJiApp 根视图=RootView；占位页含「盘迹」与 API 地址展示；scheme BuildableName=PanJi.app（运行态显示待 macOS 目视） |
| 5b. baseURL 注入正常 | 静态 PASS：xcconfig `API_BASE_URL` → Info.plist `APIBaseURL=$(API_BASE_URL)` → AppConfig 读取 → RootView 展示，四环节键名一致 |
| 5c. xcconfig 切换方式符合 README | PASS：README 指向 `Config/Config.xcconfig`、说明 `/$()/` 写法、契约引用为 `docs/API_CONTRACT.md` |
| 5d. Info.plist / ATS 符合当前规格 | PASS：`NSAllowsLocalNetworking=true`（ARCHITECTURE §2）、仅竖屏（裁决 B2）、DisplayName=盘迹、UILaunchScreen、LSRequiresIPhoneOS |
| 6. 保存构建命令、构建结果与验证证据 | 已保存：§12 runbook + §11 全部检查输出（构建结果=NOT EXECUTED，如实记录） |
| 7. 如有错误只修复 M0-003 范围问题 | 无需修复：静态校验 20/20 PASS，未发现 M0-003 范围内错误 |
| 8. 更新交接单 | 已更新（本单 §11/§12/§13/§20/§21） |
| 9. 状态 → REVIEW | 按规则改为 **BLOCKED**（说明见下） |
| 10. 停止等待 QA | 已停止，等待总控裁决 |

**状态说明**：总控指令第 9 条「完成后将 M0-003 改为 REVIEW」以「构建验证完成」为前提；本环境无法产出构建证据，任务不满足 REVIEW 前提。AGENTS §5：依赖未满足 → BLOCKED（阻断项=构建证据需 macOS/Xcode 执行环境）。若总控裁定由 QA 在 M0-006 执行构建（此前裁定），或提供 macOS 执行机，本任务即可解除 BLOCKED。
