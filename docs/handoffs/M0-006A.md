# M0-006A 交接说明（Database：macOS/Xcode 构建环境准备）

## 任务编号

M0-006A【DBI】macOS/Xcode 构建环境准备（环境能力任务，非代码任务）

## 角色

Database / 基础设施（DBI）——环境获取与就绪牵头

## 完成状态

**BLOCKED（阻塞类型：环境能力）**——环境未获得：本执行环境无真实 Mac、无 macOS CI 能力；如实报告 REAL_MAC_UNAVAILABLE，不伪造任何构建证据。待总控裁决环境方案。

## 前置阅读（已完成）

`/AGENTS.md`、`docs/TASKS.md`、`docs/ARCHITECTURE.md`、`docs/handoffs/M0-003.md`（§12 runbook 已确认）、`docs/handoffs/M0-003-QA.md`、`infra/**`。

## 【环境类型】

**未获得环境（REAL_MAC_UNAVAILABLE）**

- 真实 Mac：不可用。本 Agent 执行环境为 Microsoft Windows NT 10.0.26200（Windows 10/11 系），非 macOS；SSH 已知主机（`~/.ssh/known_hosts`）仅 `github.com`、无 `ssh config` 别名、无可达的局域网/远程 Mac。第三方远程 Mac 构建服务按任务规定"未经批准不得使用"，未采用。
- macOS CI：不可行（现状评估）。工作区 `D:\deepseek\盘迹APP` **不是 git 仓库**（无 `.git`），无 GitHub 远程，无 `.github/` 目录——CI 工作流即使落盘也无法在任何 macOS runner 上执行；且 B11 过渡方案（允许 CI 替代真实 Mac）未见总控批准记录。两个条件均不满足，故不创建 CI 文件。

## 【macOS版本】

不适用（执行环境为 Windows；`sw_vers` ABSENT）

## 【Xcode版本】

不适用（`xcodebuild` / `xcode-select` ABSENT，无任何 Xcode 安装）

## 【xcodebuild状态】

**ABSENT**（Get-Command 探测无此命令；Windows 环境物理不可执行）

## 【Simulator状态】

**ABSENT**（`xcrun` / `simctl` ABSENT，无 iOS Simulator；无法启动模拟器）

## 【Docker状态】

**DOCKER_AVAILABLE = NO**（`docker` ABSENT，无 Docker Desktop 安装路径与服务）。M0-006 非 macOS 段（M0-001 Docker 实跑）同样需要装有 Docker 的机器，本机不能承担，未替 QA 执行 M0-006 正式验收。

## 【执行命令】

```powershell
# 1) 执行环境与 macOS 工具探测
[System.Environment]::OSVersion.VersionString
Get-Command xcodebuild, xcrun, sw_vers, xcode-select, simctl -ErrorAction SilentlyContinue
# 2) 真实 Mac 可达性探测
Test-Path "$env:USERPROFILE\.ssh"                      # True
Get-Content "$env:USERPROFILE\.ssh\known_hosts"        # 仅 github.com
Test-Path "$env:USERPROFILE\.ssh\config"               # False
Get-Command ssh -ErrorAction SilentlyContinue          # C:\Windows\System32\OpenSSH\ssh.exe（无目标主机）
# 3) macOS CI 可行性探测
Test-Path 'D:\deepseek\盘迹APP\.git'                   # False（非 git 仓库）
Test-Path 'D:\deepseek\盘迹APP\.github'                # False（无 CI 目录）
git -C 'D:\deepseek\盘迹APP' remote -v                 # 无输出（无远程）
# 4) Docker 探测
Get-Command docker -ErrorAction SilentlyContinue       # ABSENT
```

## 【执行结果】

```
OS: Microsoft Windows NT 10.0.26200.0
xcodebuild / xcrun / sw_vers / xcode-select / simctl : 全部 ABSENT
~/.ssh/known_hosts : 仅 github.com；无 ssh config；无可达 Mac
.git : False（工作区非 git 仓库）；.github : False
docker : ABSENT
```

结论：四项环境能力（macOS、Xcode、Simulator、Docker）在本机全部缺失；真实 Mac 无任何访问通道。

## 【是否具备运行 M0-003 runbook 的条件】

**不具备。** M0-003.md §12 的 xcodebuild/模拟器 runbook 无法在本机执行；runbook 本身保持有效，等待获得 macOS 执行机后由 iOS 或 QA 直接复用。

## 【发现的代码问题】

无（构建未执行，未暴露 Swift/Xcode 工程代码问题，故无 M0-003_CODE_BLOCKER 记录）。apps/ios 静态校验已由 iOS/QA 完成（20/20 PASS + pbxproj/scheme 结构校验），构建级问题需真实 Xcode 首次构建才能暴露。

## 【未验证项】

1. xcodebuild 完整 Build（M0-003 验收①，待 macOS 执行机）
2. 模拟器启动与占位页目视（验收②，待 macOS 执行机）
3. 产物 Info.plist 中 `APIBaseURL` 与 xcconfig 注入一致（验收③，待构建后核对）
4. baseURL 切换复验（改 xcconfig 后重构建，待 macOS 执行机）
5. M0-006 非 macOS 段：M0-001 Docker 实跑、API health 复核（QA 执行，需 Docker 机器）

## 【下一步交给谁】

**总控（Lead）——环境方案裁决**，二选一：

- **首选（符合 B11）**：提供/指定一台真实 Mac（macOS + Xcode 15+，可装 iOS Simulator），DBI 复核环境能力后移交 iOS/QA 按 M0-003.md §12 runbook 执行并回填 M0-003 验收①②。
- **过渡方案（需总控明确批准 B11-CI）**：建立 GitHub 仓库并允许 macOS CI。获批后 DBI 交付 `.github/workflows/ios-build.yml`（macos runner：xcodebuild -scheme PanJi -destination 'platform=iOS Simulator,name=iPhone 15' build + 日志与 .app 产物归档 + Info.plist APIBaseURL 核对），仅证明"真实 Xcode 编译能力"，不替代模拟器人工验收与 M1-012 真机验收。

获批任一方案后：iOS/QA 执行构建 → QA 复核证据回填 M0-003 验收①② → M0-003 解除 BLOCKED → M0-006A 置 DONE（届时改 REVIEW 走审核）。

## 数据风险

无（本任务零代码、零数据库操作；未建 CI 文件、未改 apps/**、未改契约）。

## 变更记录

| 时间 | 变更 |
|---|---|
| 2026-08-19 | DBI 环境核查完成：REAL_MAC_UNAVAILABLE；CI 不可行（非 git 仓库、无远程、B11-CI 未批）；Docker ABSENT；状态 → BLOCKED，待总控裁决 |
