# M0-001 交接说明（Database：本地 PostgreSQL 环境）

## 1. 任务编号

M0-001【DBI】本地 PostgreSQL 环境（原 T-001，编号迁移；本单替代原 docs/handoffs/T-001.md，该文件已删除，同 M0-003 更名先例）

## 2. 角色

Database / 基础设施（DBI）

## 3. 完成状态

REVIEW（两轮整改完成：首轮整改引用并出交接单；第二轮按总控再次派发复核——infra 引用扫描零过期、三文件一致性复查通过、Docker 仍不可用故实跑 ①②③ 仍待 M0-006。QA 审核单 M0-001-QA.md 两项阻断中"README 过期引用"已闭环，"实跑证据"受本机环境限制未变）

## 4. 完成内容

1. **引用整改**：infra 内过期引用全部统一——`T-001` → `M0-001`；`CONTRACT-001` → `docs/API_CONTRACT.md` / `docs/DATABASE_SCHEMA.md`（README 标题、持久化验证注释、重要约定两条、docker-compose 顶部注释）。整改后 infra 目录 grep 无 CONTRACT/T-001/T-101/DB-001 残留（仅保留现行文档名引用）。
2. **三文件一致性核对**（逐项通过，见测试结果）。
3. **Docker 实跑验证**：本环境未安装 Docker（探测证据见测试结果），无法执行启动/psql/持久化三条验证；验证命令已固化在 infra/README.md「验证」节，供 M0-006 在装有 Docker 的机器执行。

## 5. 修改文件

| 文件 | 变更 |
|---|---|
| `infra/README.md` | 标题与引用整改（M0-001、docs/API_CONTRACT.md、docs/DATABASE_SCHEMA.md） |
| `infra/docker-compose.yml` | 顶部注释 `T-001` → `M0-001` |

## 6. 新增文件

`docs/handoffs/M0-001.md`（本单）。原 `docs/handoffs/T-001.md` 已删除。

## 7. API 变化

无。

## 8. 数据库变化

无。infra 仍只创建空库（`panji`），不执行任何建表 SQL；业务表由 BE TypeORM migration 落库（API_CONTRACT 裁决 A1）。

## 9. UI 变化

无。

## 10. 测试命令

Docker 可用性探测（本机执行）：

```powershell
docker --version
docker compose version
docker info --format '{{.ServerVersion}}'
Test-Path 'C:\Program Files\Docker\Docker\Docker Desktop.exe'
Test-Path 'C:\Program Files\Docker\Docker\resources\bin\docker.exe'
Get-Service -Name "*docker*"
```

实跑验证命令（infra/README.md「验证」节，待 Docker 机器执行）：

```bash
cd infra && docker compose up -d
docker compose ps
docker exec -it panji-postgres psql -U panji -d panji -c "select version();"
docker exec -it panji-postgres psql -U panji -d panji -c "create table _t(id int); insert into _t values (1);"
docker compose down && docker compose up -d
docker exec -it panji-postgres psql -U panji -d panji -c "select * from _t; drop table _t;"
```

## 11. 测试结果

**A. Docker 探测（本机，2025-XX-XX）**：全部失败——`docker` 命令 not recognized（exit 1）；`Docker Desktop.exe` / `docker.exe` 路径均不存在（Test-Path = False）；无 docker 服务；podman、psql 亦不存在。结论：本机无法执行 M0-001 验收①②③。

**B. 三文件一致性核对（逐项通过）**：

| 项 | docker-compose.yml | .env.example | README | 一致 |
|---|---|---|---|---|
| 用户 | `${POSTGRES_USER:-panji}` | `panji` | `panji` | ✅ |
| 密码 | `${POSTGRES_PASSWORD:-panji_dev}` | `panji_dev` | `panji_dev` | ✅ |
| 库名 | `${POSTGRES_DB:-panji}` | `panji` | `panji` | ✅ |
| 端口 | `${POSTGRES_PORT:-5432}:5432` | `5432` | `5432` | ✅ |
| 连接串 | — | — | `postgresql://panji:panji_dev@localhost:5432/panji`（= ARCHITECTURE §6 DATABASE_URL） | ✅ |
| 镜像 | `postgres:16-alpine` | — | PostgreSQL 16 | ✅ |
| 容器名 | `panji-postgres` | — | docker exec 命令同用 `panji-postgres` | ✅ |
| 持久化 | named volume `panji_pgdata` | — | 持久化验证章节 | ✅ |
| healthcheck | `pg_isready -U panji -d panji` | — | — | ✅ |

**C. 第二轮复核（2026-08-19，总控再次派发后）**：

- infra 引用扫描：`Select-String` 模式 `CONTRACT|T-001|T-101|DB-001` → 命中 0 处过期引用；剩余命中均为现行文档名（docs/API_CONTRACT.md、docs/DATABASE_SCHEMA.md）。
- Docker 复查：`docker --version` / `docker compose version` → CommandNotFoundException（exit 1）；`C:\Program Files\Docker\Docker\Docker Desktop.exe`、`...\resources\bin\docker.exe` → Test-Path **False**；无 docker 服务。结论：Docker 仍不可用，验收 ①②③ 无法在本机执行。
- 环境事实（新增）：本机存在预装 PostgreSQL `D:\wzw\.pgsql`（`bin\psql.exe` 存在，5432 被其占用，与 BE M0-002 §13.1 一致）。该实例非 Docker，不能替代 M0-001 的 compose/named-volume 验收口径。
- 三文件一致性：本轮逐行重读 README（54 行）/ docker-compose.yml（22 行）/ .env.example（5 行），§11B 对照表逐项仍通过，无回归。

## 12. 构建结果

不适用（infra 无编译产物；compose YAML 22 行已人工逐行核对，无模板语法问题）。

## 13. 已知问题

- 本机无 Docker/psql，M0-001 验收①②③未在本机执行；验证命令已固化于 README，待 M0-006 实跑。
- `README` 中 `cp .env.example .env` 为 PowerShell 兼容写法（cp 为 Copy-Item 别名），Linux/macOS 同样适用。
- **跨模块发现（M1-001 前必须处理）**：BE 已在 M0-002 预置 `apps/api/src/migrations/1787126142122-CreateTables.ts` 与 6 实体，均按 DATABASE_SCHEMA **v1** 口径落地——`items` 无 `deleted_at`、`item_records.item_id` FK 为 `ON DELETE CASCADE`。当前 DATABASE_SCHEMA 已升 **v2**（items 逻辑删除 + FK RESTRICT），两者脱节。需总控批准 SCHEMA_CHANGE_PROPOSAL-001 后，由 BE 在 M1-001 同步迁移与实体（DBI 不改 apps/**）。

## 14. 风险

- M0-006 执行机若无 Docker，将阻塞 M0 放行（建议 QA 提前确认环境）。
- BE 侧 `.env` 数据库账号密码需与 `panji/panji_dev` 一致（总控已在 M0-002 预审中指出并列入整改）。
- 若真机联调改过 POSTGRES_PORT，连接串需同步 `.env`，README 已说明。
- SCHEMA_CHANGE_PROPOSAL-001 若未在 M1-001 派发前批准，BE 预置迁移将以旧口径（硬删除+CASCADE）合入，删除玩物会物理抹掉多年记录——必须先行裁决。

## 15. 对其他角色影响

- BE：连接串不变；`DATABASE_URL` 仍为 `postgresql://panji:panji_dev@localhost:5432/panji`。
- QA（M0-006）：按 infra/README.md「验证」节三条命令实跑并留存输出。
- 无契约/无 apps 影响（本次未改 API_CONTRACT、未建表、未动 apps/**）。

## 16. 建议下一任务

M0-006（QA 实跑代验 M0-001 ①②③，并复核本单 README 引用整改）→ 总控置 M0-001 DONE；**先批准 SCHEMA_CHANGE_PROPOSAL-001**，再派发 M1-001（BE 按 DATABASE_SCHEMA v2 同步预置迁移/实体：items 加 deleted_at、item_records FK 改 RESTRICT），前置依赖 M0-002。
