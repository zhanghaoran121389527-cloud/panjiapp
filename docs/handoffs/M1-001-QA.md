# M1-001 QA 审核交接单（QA）

- 审核对象：M1-001【BE】数据模型实体 + 迁移 + 种子（6 表）
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-19（同日）
- 审核依据：DATABASE_SCHEMA v2（逻辑删除口径）、API_CONTRACT v2.3、SCHEMA_CHANGE_PROPOSAL-001（APPROVED）、TASKS M1-001 验收条件
- 审核方式：实际文件逐行核对 + 独立专用数据库实跑验证（不采信 BE 自述）

## 实际检查文件

- `apps/api/src/entities/`：base / user / auth-identity / category / item / item-record / record-image（7 文件）
- `apps/api/src/migrations/1787132713236-CreateTables.ts`、`1787132714000-SeedCategories.ts`
- `apps/api/src/data-source.ts`（migrations glob、synchronize=false、UTC）、`src/app.module.ts`（autoLoadEntities）
- 旧硬删除迁移 `1787126142122/1787126143000` 已不存在（实盘 glob 仅 2 个迁移文件）✓

## 六表检查

- 实库 inspection（独立库 panji_m1001）：public schema 恰 7 表 = users / auth_identities / categories / items / item_records / record_images + TypeORM `migrations` 表。
- **无 posts/comments/likes/follows/notifications/messages/commerce 等任何未批准表** ✓。

## deleted_at检查

- `items.deleted_at`：`timestamp with time zone`、`nullable=YES`、`column_default=NULL` —— 与 DATABASE_SCHEMA v2 完全一致 ✓。
- 实体为显式 `@Column`（非 @DeleteDateColumn 隐式软删），符合 §9.7 ✓。

## FK矩阵（实库 pg_constraint 实测）

| FK | 正式要求 | 实测 | 结果 |
|---|---|---|---|
| auth_identities → users | CASCADE | CASCADE | PASS |
| items → users | RESTRICT | RESTRICT | PASS |
| items → categories | RESTRICT | RESTRICT | PASS |
| item_records → items | **RESTRICT** | **RESTRICT** | PASS |
| record_images → item_records | CASCADE | CASCADE | PASS |

FK 总数 = 5，无多余、无缺失 ✓。

## Unique（实库 3 处，语义一致）

1. `uq_auth_identities_provider_identifier(provider, identifier)` ✓
2. `categories.name`（`UQ_8b0be…(name)`）✓
3. `uq_record_images_record_sort(record_id, sort_order)` ✓

## Index（实库 2 个）

- `idx_items_user_id`、`idx_item_records_item_id` ✓（无多余业务索引）。

## CHECK（实库恰 6 个，语义与 A12 一致）

1. users.nickname IS NULL OR char_length 1~20 ✓
2. items.name char_length 1~50 ✓
3. item_records.content char_length 1~500 ✓
4. item_records.duration_minutes IS NULL OR 1~1440 ✓
5. item_records.method IS NULL OR char_length 1~20 ✓
6. record_images.sort_order >= 0 ✓

## Migration验证（独立专用库 panji_m1001，全程实跑）

1. 空库完整迁移：`migration:run` 执行 CreateTables + SeedCategories 两个迁移 ✓
2. 迁移后结构与 DATABASE_SCHEMA v2 逐字段一致（列集合/类型/nullable/默认值 inspection）✓
3. 二次 `migration:run`：0 个待执行（幂等）✓
4. 无旧硬删除结构残留（items 无其他删除列；records→items 为 RESTRICT 非 CASCADE）✓
5. 不创建 M2 数据结构（表清单见上）✓

## Seed验证（实库）

1. categories 5 条：固定 UUID + 名称/顺序 = 核桃1/菩提2/木质3/玉石4/其他5，与 DATABASE_SCHEMA §3.1 逐条一致 ✓
2. `INSERT … ON CONFLICT (id) DO NOTHING`：重复插入后仍 5 行（幂等）✓
3. 无社区/商城假数据（仅 5 品类行）✓
4. 运行时佐证：GET /v1/categories 返回与种子完全一致（此前冒烟）。

## 逻辑删除数据库防线（数据层能力实测）

- 有 item_records 的 item 执行物理 `DELETE FROM items` → **被 FK RESTRICT 拒绝（SQLSTATE 23503）** ✓
- `UPDATE items SET deleted_at = now()` → **成功置位** ✓，且 item_records/record_images 行数不变（物理保留）✓
- 附加级联验证：物理删 record 行 → 其 record_images 级联消失（CASCADE 生效）✓

## Rollback验证（实库）

- `migration:revert` ×2（先 Seed 后 CreateTables）：回滚后 public 仅剩 `migrations` 表，无残缺结构 ✓
- 随后 `migration:run` 完整重建：2 迁移执行、7 表恢复、种子恢复 ✓
- 验证专用库已销毁，仓库无残留脚本。

## 测试结果（QA 现场实跑，工作目录 apps/api）

| 项 | 命令 | 结果 |
|---|---|---|
| lint | `node node_modules/eslint/bin/eslint.js "src/**/*.ts" "test/**/*.ts" "scripts/**/*.mjs"` | PASS（exit 0；BE 已把 scripts glob 修成 .mjs，M0-007 待办内容实际已生效） |
| typecheck | `tsc --noEmit` | PASS（exit 0） |
| build | `nest build` | PASS（exit 0） |
| unit | `jest --runInBand` | PASS（1 套件 6 用例） |
| e2e | `jest --config test/jest-e2e.json` | 28/29：唯一失败 = `DELETE /items/:id` 旧硬删除断言（见下） |
| 数据层专项 | 临时脚本（独立库，已删） | **ALL PASS**（上述全部 PASS 项） |

## 已知但不属于本任务的问题（划归 M1-003 / 其他）

1. `items.service.ts` remove() 仍物理删除、list/getById/findOwned 未过滤 `deleted_at IS NULL`——**M1-003**（总控已裁定）。
2. `test/main-chain.e2e-spec.ts:431-453` DELETE 用例仍按旧硬删除断言（实测 500，恰为数据层 RESTRICT 正确生效的证明）——**M1-003** 改为逻辑删除断言。
3. `docs/handoffs/M1-001.md` §64 写"重复删除仍 204"，与 API_CONTRACT v2.3 §3.9 / SCHEMA_CHANGE_PROPOSAL-001 §7 最终裁定"**重复删除→404**"矛盾——提醒 BE 在 M1-003 以 v2.3 为准，勿照抄该行。
4. `docs/DATABASE_SCHEMA.md` §8 仍写 `pnpm migration:run`（团队已统一 npm）——文档小修，非本任务代码。
5. M0-006A（Docker 机器）具备后，建议将本单数据层验证在 Docker PG 上复跑一遍。

## 阻断问题

**无。**

## 最终结论

**PASS**

## 是否允许 M1-001 置 DONE

**YES** —— 建议总控将 M1-001 置 DONE；随后按依赖解锁 M1-002 / M1-003（M1-003 需按 v2.3 逻辑删除实施并修复上述第 1、2 项）。

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M1-001 数据层正式审核，独立库全量验证 ALL PASS |
