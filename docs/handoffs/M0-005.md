# M0-005 交接单（QA：M1 验收清单与测试计划 v2）

## 1. 任务编号

M0-005【QA】M1 验收清单与测试计划

## 2. 负责人角色

QA

## 3. 完成状态

REVIEW（v2 提交，待总控审核）

## 4. 完成内容

- `docs/qa/验收清单.md` 由 v1（已退回）全面重写为 v2，对齐最新基线：
  - 主链验收 A01~A20，与 PRODUCT_SPEC §4 的 20 步一一对应（含空态 A05、创建后自动进详情 A10、"记录第一天"入口 A11、多图 A12、补录 A16、排序 A17、重启 A18~A20）。
  - 异常/边界 B01~B21 共 21 项（≥15 要求）：无网络/断网/超时/400/401/404/500/上传失败与取消/连点防重/空名超长（A12 全表）/无封面/多图顺序/补录排序/早于入手日期（A11 行为）/晚于今天+时区（A10+A13）/删除已有记录玩物/登录失效/用户隔离/上传类型大小/错误返回统一。
  - 持久化专项 C01~C05：杀进程/后端重启/容器重建/图片跨重启/登录态跨重启。
  - 编辑/删除玩物冒烟 E01~E07：字段编辑/换封面/null 清空/编辑校验/删除确认/级联删除/越权 404。
  - 契约核对表 §5：API 3.1~3.12 逐条（`/v1/` 前缀）、数据库层逐条（对照 `docs/DATABASE_SCHEMA.md`）、iOS 层逐条。
  - §6 已知限制：孤儿文件不回收（A15）、服务端幂等不做（A14）、M1 不做清单（业务规则 9）。
  - 旧引用全部清除：不再出现 CONTRACT-001、`/api/v1`、19 步、T-xxx 旧编号（见测试结果自校验）。
- 按总控指令，将 M0-005 状态置为 REVIEW（`docs/TASKS.md` 两处状态 + 审核记录行）。

## 5. 修改文件

| 文件 | 内容 |
|---|---|
| `docs/qa/验收清单.md` | v1 → v2 全面重写（见上） |
| `docs/TASKS.md` | M0-005 状态 IN_PROGRESS → REVIEW（总览表 + 任务详情）；任务详情追加 v2 审核记录行 |

## 6. 新增文件

- `docs/handoffs/M0-005.md`（本文件）

## 7. API 变化

无。未新增/修改任何接口；清单仅引用 API_CONTRACT v2.2 现有契约。

## 8. 数据库变化

无。

## 9. UI 变化

无。

## 10. 测试命令（v2 清单自校验，Windows 环境）

```powershell
# 旧引用清零检查（应全部 0 命中）
Select-String -Path 'docs\qa\验收清单.md' -Pattern 'CONTRACT-001','/api/v1','T-0','T-1','T-2','19 步','record_images 不建','无删除' | Measure-Object
# 关键新基线命中检查（应全部 ≥1 命中）
Select-String -Path 'docs\qa\验收清单.md' -Pattern '/v1/','photoUrls','PATCH','DELETE','GET /v1/items/:itemId/records','A10','A18','DATABASE_SCHEMA','DESIGN_SYSTEM','PRODUCT_SPEC' | Measure-Object
# 结构计数（应：A=20 行、B=21 行、C=5 行、E=7 行）
(Select-String -Path 'docs\qa\验收清单.md' -Pattern '^\| A\d{2} \|').Count
(Select-String -Path 'docs\qa\验收清单.md' -Pattern '^\| B\d{2} \|').Count
(Select-String -Path 'docs\qa\验收清单.md' -Pattern '^\| C\d{2} \|').Count
(Select-String -Path 'docs\qa\验收清单.md' -Pattern '^\| E\d{2} \|').Count
```

## 11. 测试结果

| 检查 | 结果 |
|---|---|
| 旧引用（CONTRACT-001、/api/v1、T-xxx 旧编号、19 步等） | 0 命中 |
| 新基线关键引用（/v1/、photoUrls、PATCH、DELETE、GET records、A10~A18、DATABASE_SCHEMA、DESIGN_SYSTEM、PRODUCT_SPEC） | 全部命中 |
| 主链 A 条目数 | 20 |
| 异常/边界 B 条目数 | 21（≥15 达标） |
| 持久化 C 条目数 | 5 |
| 编辑/删除冒烟 E 条目数 | 7 |

## 12. 构建结果

不适用（纯文档任务，无代码构建）。

## 13. 已知问题

- 无。`docs/DESIGN_SYSTEM.md` 已 v1 定稿（M0-004 DONE），清单 §0 按 v1 引用，视觉走查依据明确。
- M0-006 执行需要 Docker（M0-001 实跑）与 macOS/Xcode（M0-003 构建）环境；当前 QA 本机两者均无，已在清单 §0 与 M0-001-QA/M0-003-QA 中标注。

## 14. 风险

- 若 M0-006 前仍无 Docker/macOS 环境，M0 闸门无实证支撑，M1 不得放行（QA 立场：NOT VERIFIED 不放行）。
- 契约如再变更，本清单需同步修订；未经总控确认不得私下改清单条目。

## 15. 对其他角色影响

- BE：契约核对表 §5.1/§5.2 即 M1-001~M1-005 的 QA 勾选依据。
- iOS：§1 主链与 §2 B 项即 M1-006~M1-011 的 QA 冒烟依据；§4 E 项即 M1-011 冒烟依据。
- UI：视觉走查依据 DESIGN_SYSTEM v1 已定稿（M0-004 DONE），M1 起 iOS 页面按 §5.3 与线框核对。
- DBI：C03 与 M0-001 验收③联动。
- 总控：本清单是 M0-006 / M1-012 唯一验收依据。

## 16. 建议下一任务

- 总控审核 M0-005（通过后置 DONE）。
- 现状：M0-002（BE 底座）与 M0-004（UI 规格）已 DONE；M0-001/M0-003 经 QA FAIL-hold（缺实跑/构建证据），实跑与构建验证已并入 M0-006。总控放行 M0-006 前，需提前准备 Docker 机器（代验 M0-001 ①②③）与 macOS/Xcode 机器（代验 M0-003 构建）。
- 按总控指令，本轮 QA 不自行启动 M0-006。

## 17. 变更记录

| 时间 | 变更 |
|---|---|
| 建立日 | v2 提交（v1 退回后重写），M0-005 → REVIEW |
