# M0-001 QA 审核交接单（QA）

- 审核对象：M0-001【DBI】本地 PostgreSQL 环境（`infra/`）
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-19（同日）
- 审核依据：TASKS M0-001 任务书、ARCHITECTURE §6、API_CONTRACT v2.2、DATABASE_SCHEMA §0、AGENTS §6
- 审核方式：实际文件逐行核对 + 本机实跑尝试

## 1. 实际文件核对（静态）

| 项 | 契约要求 | 实际 | 结果 |
|---|---|---|---|
| 镜像 | PostgreSQL 16 | `postgres:16-alpine` | PASS |
| 账号 | panji / panji_dev / panji | compose 默认值与 .env.example 一致 | PASS |
| 端口 | 5432 | `5432:5432`（可经 .env 覆盖） | PASS |
| 持久化 | named volume | `panji_pgdata:/var/lib/postgresql/data` | PASS |
| 健康检查 | 有 | pg_isready，interval 5s，retries 10 | PASS |
| 建表 SQL | 禁止（A1，infra 只建空库） | 无任何 SQL | PASS |
| 连接串 | `postgresql://panji:panji_dev@localhost:5432/panji` | README 与实际一致 | PASS |
| 持久化验证命令 | 写入→down→up→数据仍在 | README §验证 第三条齐全 | PASS（未执行） |

## 2. 实跑验证（本机）

- `docker --version` / `docker compose version` / `psql --version`：**全部不存在**（Windows 本机未装 Docker）。
- 验收条件 ①②③（一条命令启动 / psql 可连 / 容器重建后数据仍在）**无法在本机执行**。
- 与总控裁决一致：实跑并入 M0-006（需装有 Docker 的机器）。

## 3. 发现的问题

### 必须修复（阻断项）

1. **[阻断] 验收条件 ①②③ 无任何实跑证据**：NOT VERIFIED。DBI 交接单自述"无法实跑"，本机复核属实。在 M0-006 于 Docker 机器实跑通过前，不得置 DONE。
2. **[轻量] infra/README.md 契约引用过期**：正文两处引用 `CONTRACT-001`（裁决 A1/A8），该文档已更名为 `docs/API_CONTRACT.md`；标题与命令注释仍写 `T-001`（现行编号 `M0-001`）。BE/QA 按 README 追踪契约会迷路，须在 M0-006 实跑前一并整改。

### 建议优化

- compose 使用固定 `container_name: panji-postgres`，同机多套环境会冲突；开发期可接受，不做要求。

## 4. 范围与红线

- DBI 仅产出 `infra/**`（+ 被总控提升的 DATABASE_SCHEMA），未动 `apps/**`、未执行建表 SQL、未改契约。范围合规，无膨胀。

## 5. 最终结论

**FAIL（阻止置 DONE，暂缓合入）**

- 唯一实质阻断 = 验收条件全部未实跑（NOT VERIFIED）。
- 处置：任务保持 REVIEW → M0-006 在装有 Docker 的机器按 infra/README 三条命令实跑并留输出 → 连同 README 契约引用整改后，QA 复核置 DONE。

## 6. 证据留存

- `docker --version` → CommandNotFoundException
- `docker compose version` → CommandNotFoundException
- `psql --version` → CommandNotFoundException
- infra/docker-compose.yml（22 行）、.env.example（5 行）、README.md（54 行）已逐行核对。
