# M0-003 QA 审核交接单（QA）

- 审核对象：M0-003【iOS】SwiftUI 工程底座（`apps/ios/`）
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-19（同日）
- 审核依据：TASKS M0-003 任务书、ARCHITECTURE B1/B2/B3、API_CONTRACT §1、DESIGN_SYSTEM v0、AGENTS §6
- 审核方式：14 个文件逐一实读 + pbxproj/scheme/plist 独立结构校验；构建无法执行（Windows 无 Xcode）

## 1. 实际文件核对（静态，全部基于实读文件而非交接单自述）

| 项 | 要求 | 实际 | 结果 |
|---|---|---|---|
| 目录布局 | apps/ios/（B1） | 根级 `ios/` 已不存在，`apps/ios/` 就位 | PASS |
| baseURL 注入链 | xcconfig → Info.plist → AppConfig | Config.xcconfig 定义 `API_BASE_URL`；4 个 XCBuildConfiguration 均挂 `baseConfigurationReference`；Info.plist `APIBaseURL=$(API_BASE_URL)`；AppConfig 读取并空值 preconditionFailure | PASS |
| API 前缀口径 | `/v1/`（A9） | xcconfig 注释与 README 均明确"根地址不含路径，/v1/ 由网络层拼接" | PASS |
| 仅 iPhone（B2） | TARGETED_DEVICE_FAMILY=1 | 项目+Target 四个配置均为 1 | PASS |
| 锁竖屏（B2） | 仅 Portrait | Info.plist UISupportedInterfaceOrientations 仅 Portrait 一项 | PASS |
| 最低 iOS 17.0（B2） | IPHONEOS_DEPLOYMENT_TARGET=17.0 | 四个配置一致 | PASS |
| Bundle ID（B3） | com.panji.app | PRODUCT_BUNDLE_IDENTIFIER=com.panji.app | PASS |
| ATS | 局域网 http 例外 | NSAllowsLocalNetworking=true | PASS |
| 主题 token | DESIGN_SYSTEM v0 四色 | #F6F1E7/#4A3728/#8A7B6D/#8C6D4F 与 RGB 换算逐一相符 | PASS |
| README 契约引用 | docs/API_CONTRACT.md | 第 32 行引用正确 | PASS |
| 业务页面 | 禁止（M1-006 起） | 仅 RootView 占位页；无 Features/ 目录、无网络请求、无登录逻辑 | PASS |
| 文件清单 | 交接单 14 个 | 实盘 14 个，无多无少，无隐藏文件 | PASS |

## 2. pbxproj / scheme 独立结构校验（QA 自己执行，非采信交接单）

- 括号配平 26/26；UTF-8 头 `// !$*UTF8*$!`；objectVersion 56。
- 对象 33 个（32 带注释头 + 1 个无注释 mainGroup `B1…01`，已确认定义于第 39 行），ID 全唯一。
- 引用闭合：mainGroup、productRefGroup、target、buildPhase、4 个 Swift 源文件、Assets、Info.plist、Config.xcconfig 引用全部有定义；scheme BlueprintIdentifier 指向已定义 target。
- Sources 阶段挂 4 个 Swift 文件、Resources 阶段挂 Assets.xcassets、**Info.plist 未混入 Resources 阶段**（正确，避免 Multiple commands produce）。
- 全部 9 个文本文件严格 UTF-8 通过；3 个 Contents.json 可解析；AppIcon-1024.png 存在。
- 本机无法执行 `xcodebuild`（Windows）。

## 3. 发现的问题

### 必须修复（阻断项）

1. **[阻断] 构建与运行零证据（NOT VERIFIED）**：验收条件①（Xcode 构建成功附日志）②（模拟器运行显示占位页与注入地址）完全未执行。工程为手写 pbxproj，从未被 Xcode 实际打开/编译过；交接单自述"尚未完成 Xcode 构建"。按 AGENTS §6，不接受"静态校验通过=完成"。

### 建议优化（不阻断）

- 注释/README 编号过期：`PanJiTheme.swift` 注释、`RootView.swift` 注释、`apps/ios/README.md` 中仍写 `T-004`/`T-106`/`tokens.md`，现行编号为 `M0-004`/`M1-006`，文档名为 `DESIGN_SYSTEM.md`。M1-006 起替换主题时顺带清理。
- `AppConfig` 使用 `preconditionFailure`：M0 占位期可接受，M1-006 网络层接入后建议改为可恢复的错误处理（届时该文件本就会被重构）。

## 4. 范围与红线

- 无社区/帖子/点赞/关注/消息/商城/AI/会员/积分/Android 任何痕迹；无网络层、无业务页面；未改契约。范围合规。

## 5. 最终结论

**FAIL（阻止置 DONE，暂缓合入）**

- 唯一实质阻断 = 验收条件①②零构建证据（NOT VERIFIED）。
- 处置：任务保持 REVIEW → M0-006 在 macOS 环境执行 `xcodebuild` 构建 + 模拟器运行并留存日志/截图 → QA 复核后置 DONE。

## 6. 证据留存

- 本机无 Xcode：`xcodebuild` 不可执行。
- QA 独立校验脚本输出：pbxproj 26/26 括号配平、33 对象全唯一、0 未定义引用、scheme target 一致、9 文件 UTF-8 全过、3 JSON 全过。
