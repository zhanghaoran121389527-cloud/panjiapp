# M1-010 QA 审核交接单（QA）

- 审核对象：M1-010【iOS】盘玩记录（多图 + 补录）
- 审核角色：QA（独立否决权）
- 审核时间：2026-09-05（同日）
- 审核依据：API_CONTRACT v2.3 §3.10/3.11、docs/design/06-record.md、DESIGN_SYSTEM v1（§6.8/§6.4）、验收清单 v2.1、TASKS M1-010 验收条件
- 编号映射说明：任务书 QA 项的「B02（无照片保存）/B04（断网/上传失败）/B14（未来日期灰置）」对应现行清单 v2.1 的 **B13（多图与无图纯文字可保存）/B02+B08（断网表单保留/上传失败）/B16（晚于今天+客户端上限）**，本单按此映射执行（A 编号与清单一致，直接使用）。
- 状态说明：看板 M1-010 已为 DONE（总控已复核 CI 并批准，QA 冒烟并入 M1-012）；本单为 QA 正式复核交接补交，结论维持 DONE。

## CI 构建证据（run id + head SHA，按总控要求引用）

- **run #33948349649，head ca00872（M1-010 全部代码）真绿**：`** BUILD SUCCEEDED **`（Xcode 16.4 / iPhoneSimulator）+ 模拟器安装/launch + 截图回传 + `STEP4_ALL_OK`（总控实读回传日志确认，见 TASKS M1-010 审核记录·CI+总控）。
- QA 本地交叉复核：ci-logs 回传产物（2026-09-05 13:47:53，build 109876B / step4 / 截图 130937B）尾行为 `** BUILD SUCCEEDED **` 与 `STEP4_ALL_OK`，与绿跑一致；本地 git HEAD = `ef1fbb1`（= ca00872 + 2 个纯文档提交，应用代码无变化，`git -c safe.directory=… log` 已核）。
- 截图像素核验（1206×2622）：暖米白 90.8% / 木棕 1.5% / 深茶褐 0.3% / surface 4.9% —— 登录页特征，App 启动无崩溃 ✓。

## 主链步 A12~A16 核对（静态级）

| 步 | 内容 | 实现（实读） | 结果 |
|---|---|---|---|
| A12 | 添加照片（0~N，顺序=选择顺序） | PhotosPicker 多选；`loadTransferable` 顺序追加 → `photoStates` 顺序即展示序与 photoUrls 下标序；添加格始终在最后 | PASS |
| A13 | 输入「刚到手，先记录一下。」 | 一句话多行（axis .vertical，minHeight 88），打开即聚焦（0.4s），1~500 截断 | PASS |
| A14 | 保存 | canSubmit（trim 非空 ≤500 + !submitting）防连点；提交中「保存中…」禁用；失败内容全保留、重试只补未完成步骤 | PASS |
| A15 | 时间轴出现记录 | 保存成功 → dismiss sheet → onSaved（详情 toast「已保存」+ sensoryFeedback 1.5s）→ `onDismiss` 自动 `store.refresh()` 重拉时间轴（M1-009 接线，服务端排序） | PASS |
| A16 | 补录过去日期 | DatePicker `in: ...endOfBeijingToday()`；**未动日期不传 recordedDate**（走服务端默认北京今天）、动过传所选 YYYY-MM-DD（北京时间） | PASS |

## 冒烟项核对（静态级）

| 项 | 场景 | 实现 | 结果 |
|---|---|---|---|
| 无照片保存（→B13） | 0 张图直接保存 | canSubmit 不依赖照片；photoUrls=[] POST 3.11 | PASS |
| 断网/上传失败（→B02+B08） | 上传失败/断网 | PhotoState 四态：失败张置灰 0.45 + arrow.clockwise，行内「这张没传成功，点按重试」；点击该张**单独重传**，已成功 URL 不重传（08-states D）；不阻塞加选其他照片；断网时表单内容全保留 | PASS |
| 未来日期灰置（→B16） | 日期选择上限 | `DatePicker in: ...endOfBeijingToday()`（北京时间计算，A13），未来日期系统级灰置不可选 | PASS |
| 防连点 | 保存连点 | submitting 禁用 + canSubmit 双保险（验收⑥） | PASS |
| 放弃确认 | 未保存内容点 ✕ | confirmationDialog「放弃这次记录？」（放弃/继续编辑） | PASS |

## 10 秒体验观察项

- 静态支撑成立：打开即聚焦一句话（0.4s 延迟）、默认表单只有「照片 + 一句话 + 保存」、更多记录项默认折叠、保存成功即关 sheet 回详情。
- **实测计时并入 M1-012 真机主链**（与看板既定口径一致）。

## 工程与范围核对

- 越界申报：APIDTOs 追加（网络层 DTO 职责）+ ItemDetailView 接线（任务明确的 Items 触点）为最小必要修改，交接单已申报；Records 两文件在允许范围；pbxproj 注册 Records 分组与 2 文件（Sources 23 文件，CI 编译佐证）。无其他越界。
- 无 M1 外功能：无记录编辑/删除、无社区痕迹；HEIC 原样上传（3.10 白名单）。

## 发现的问题

- 无阻断问题。
- 观察项（并入 M1-012）：① 逐张串行上传 N 张耗时线性（M1 量级可接受，交接单已注明）；② 照片多选无数量上限（契约 0~N，服务端 ≤10MB/张兜底）；③ 北京时区工具与 CreateItemStore 各持一份（M1-011 可顺带收敛，非必须）；④ 全屏 sheet 仅 ✕ 关闭（无下滑关闭，设计既定）。

## 最终结论

**PASS**（交互级 A12~A16 实测与 10 秒计时并入 M1-012 真机主链；CI 证据 run #33948349649 @ ca00872 真绿已核）

## 是否允许 M1-010 置 DONE

**YES**（看板已 DONE，本单复核维持该结论）——M1 功能线仅剩 M1-011（编辑/删除玩物，READY）；M1-010 通过后 M1-012（20 步真机主链验收）的前置将全部就绪。

## 变更记录

| 时间 | 变更 |
|---|---|
| 2026-09-05 | 建立：M1-010 独立复核（A12~A16 + 无照片/断网上传/未来灰置 + 10s 支撑 + CI run/head 引用）→ PASS |
