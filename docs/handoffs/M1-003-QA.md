# M1-003 QA 审核交接单（QA）

- 审核对象：M1-003【BE】分类 + 玩物 CRUD（含逻辑删除）
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-25（同日）
- 审核依据：API_CONTRACT v2.3 §3.4~3.9 + 业务规则 4/10 + 裁决 A16、DATABASE_SCHEMA v2、验收清单 v2.1（B17/B18/E05~E07、§5.1）
- 审核方式：实读代码 + 独立复跑 e2e + 独立 HTTP/DB 冒烟（不采信 BE 自述）

## 规格符合度（契约核对表 3.4~3.9 逐项）

| 契约 | 检查点 | 结果 |
|---|---|---|
| 3.4 GET /v1/categories | 公开（无 token 200）、固定 5 品类按 sort_order | **PASS**（冒烟实测 核桃/菩提/木质/玉石/其他） |
| 3.5 GET /v1/items | 仅当前用户、createdAt 倒序、dayCount 服务端算、**不含已删除**（`deletedAt: IsNull()`） | **PASS**（代码 + 冒烟：删除后列表为空） |
| 3.6 POST /v1/items | name 1~50/categoryId 必填、其余 null、201 完整对象、dayCount=0 | **PASS**（冒烟实测） |
| 3.7 GET /v1/items/:id | 不含 records、越权 404、**已删除 404**（findOwned 过滤 deletedAt IsNull） | **PASS**（冒烟：已删 404、B 访问 404） |
| 3.8 PATCH /v1/items/:id | 部分更新、null 清空选填、空对象 400、**已删除 404** | **PASS**（冒烟：null 清空、已删 404） |
| 3.9 DELETE /v1/items/:id | 204=逻辑删除（UPDATE deleted_at WHERE user_id+deleted_at IS NULL）；**不存在/他人/已删除 → 404**；records/images 物理保留 | **PASS**（代码 + e2e DB 级断言 + 冒烟 DB 级核验） |
| 记录入口 | 3.11/3.12 对已删除玩物 404（经 ItemsService.findOwned） | **PASS**（冒烟：已删 records 404） |

代码实读确认：`items.service.ts` remove() 为 QueryBuilder `UPDATE … SET deleted_at=now() WHERE id=:id AND user_id=:userId AND deleted_at IS NULL`，`affected=0 → 404`（含重复删除=404，与 v2.3 最终裁定一致，未采用 M1-001 交接单 §64 旧口径）；list/findOwned 均显式 `deletedAt: IsNull()`，无隐式软删依赖。

## 测试执行（QA 独立复跑）

| 项 | 命令 | 结果 |
|---|---|---|
| e2e（独立复跑） | `DATABASE_URL=<专用库 panji_qa_m1003> jest --config test/jest-e2e.json` | **29/29 全绿**（2 套件） |
| 独立冒烟 | 自建脚本（临时文件，用后即删）：真实进程 3100 + 专用库，走 3.4~3.9 与 B17/B18/E05~E07 | **ALL PASS**（11/11） |
| lint / typecheck / build / unit | eslint / tsc --noEmit / nest build / jest --runInBand | **全绿**（0/0/0/6·6） |

冒烟关键输出（留存于本机执行记录）：

```
3.4 categories 公开+5 品类+顺序 | PASS（核桃/菩提/木质/玉石/其他）
3.6 创建完整对象 dayCount=0    | PASS
3.7 详情不含 records           | PASS
3.8 PATCH null 清空选填        | PASS
B18 隔离：B 列表仅自己/详情/PATCH/DELETE 全 404 | PASS
E05 无记录玩物删除 204，删后 404            | PASS
E06 有记录玩物删除 204                      | PASS
DB 级：item 行在 + deleted_at 置位 + records=1 物理保留 | PASS
v2.3：重复删 404/详情 404/records 404/PATCH 404/列表空 | PASS
```

## Build状态

`nest build` 成功（exit 0）。

## 发现的问题

### 环境问题（非代码缺陷，记录在案）

1. **共享库并发竞争**：首次独立复跑 e2e 时在共享 panji 库（5433）上 25 用例全红，报 `pg_class_relname_nsp_index` 目录级唯一冲突（main-chain beforeAll 的 `DROP SCHEMA public CASCADE` + `runMigrations` 与其他角色在共享库上的并发活动相撞，间歇性）。**换全新专用库后 29/29 全绿**。建议：e2e 运行前用专用库或与 BE 协调排他时段；正式 CI 时每个 job 用独立库。（非 M1-003 阻断，无需改代码。）

### 非阻断小项

2. `test/main-chain.e2e-spec.ts` describe 标题仍写"契约 v2.2"（内容已 v2.3）——纯文案，随下次修改顺带更新。
3. `docs/handoffs/M1-003.md` §13.2 提到 curl 测试数据留在共享库——开发期可接受（e2e 每次重置 schema）。

## 范围与红线

- 改动面与任务书一致：仅 `items.service.ts`（逻辑删除+查询过滤）与 `main-chain.e2e-spec.ts`（DELETE 用例改逻辑删除断言）；未动 categories/entities/migrations/uploads/契约。无范围膨胀、无未批准功能。

## 阻断问题

**无。**

## 最终结论

**PASS**

## 是否允许 M1-003 置 DONE

**YES** —— 建议总控将 M1-003 置 DONE；其前置依赖（M1-001）已 DONE，可继续解锁 M1-002 / M1-004 / M1-005 及 iOS 线（M1-007 等，前置 M0-006A 已 DONE）。

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M1-003 独立复核（e2e 29/29 + 冒烟 11/11 + 静态全绿）→ PASS |
