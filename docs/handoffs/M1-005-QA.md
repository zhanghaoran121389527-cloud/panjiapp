# M1-005 QA 审核交接单（QA）

- 审核对象：M1-005【BE】盘玩记录接口 POST/GET（多图 + 补录）
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-25（同日）
- 审核依据：API_CONTRACT v2.3 §3.11/3.12、业务规则 3/6、裁决 A10/A11/A12/A13/A18、验收清单 v2.1（B13/B14/B15/B16/B19/§5.1）
- 审核方式：实读代码（records.service/DTO/controller，M1-003 轮已读并本轮复核无变化）+ 独立 HTTP/DB 冒烟 + e2e 独立复跑（不采信 BE 自述）

## 契约核对表 3.11/3.12 逐项

| 契约点 | 实现 | 独立实测 |
|---|---|---|
| photoUrls 0~N，落 record_images，sort_order=数组下标 | 事务内 map((url,index)→sortOrder:index) | DB 级核验 sort_order=0,1 顺序保持 ✓ |
| content 必填 1~500（trim） | DTO @Length(1,500)+trim | 501 字 400；全空格 400 ✓ |
| durationMinutes 1~1440 | @Min(1)@Max(1440) | 0→400、1441→400 ✓ |
| method ≤20 | @MaxLength(20) | 21 字→400 ✓ |
| recordedDate 默认北京今天（A13，应用层） | `dto.recordedDate ?? todayBeijing()` | 不传 → 201 recordedDate=2026-08-25（北京今天）✓ |
| 过去日期=补录（同接口，A11 不校验入手先后） | 同接口 | 传昨天 → 201 ✓ |
| 晚于北京今天 → VALIDATION_ERROR（A10） | `> todayBeijing()` → 400 | 传明天 → 400 `VALIDATION_ERROR` ✓ |
| photoUrls 仅 /uploads/ 相对 URL | DTO @Matches each | （e2e 覆盖）✓ |
| 3.12 排序：recordedDate 倒序、同日期 createdAt 倒序 | order 双字段 | 实测 [今天#2, 今天#1, 昨天] ✓ |
| 归属校验（他人/已删 item 404） | 经 ItemsService.findOwned（含 deleted_at 过滤） | B GET/POST A 的 records → 404/404 ✓ |
| 响应结构 {record{…}}/{records:[…]} | 字段与契约一致（photoUrls 按 sort_order） | ✓ |
| 记录无编辑/删除接口 | 未实现（契约 M1 不做） | ✓ |

## 冒烟结果（QA 独立执行，真实进程 127.0.0.1:3100 + 专用库）

8/8 ALL PASS：
1. 默认日期=北京今天 + 多图顺序保持（201）
2. 无图可保存 photoUrls=[]（201）
3. 过去日期补录同接口（201）
4. 排序：["第二条@今天","今日盘玩@今天","昨日补录@昨天"]（同日期 createdAt 倒序）
5. 未来日期 400 VALIDATION_ERROR（A10）
6. content501/method21/duration0/duration1441/全空格 全部 400（A12）
7. 越权 GET/POST records 404/404（归属校验）
8. DB 级：record_images sort_order=0,1 与提交顺序一致（A18）

## 测试执行

| 项 | 结果 |
|---|---|
| e2e 独立复跑（专用库 panji_qa_m1005，隔离共享库并发） | **2 套件 29/29 全绿**（records 全部场景 + 已删 item records 404） |
| lint / typecheck / unit / build | 本轮代码零变更（M1-004/M1-003 轮已复跑 0/0/6·6/0） |

## 发现的问题

- 无阻断问题。
- 记录项（契约口径，非缺陷）：服务端幂等 M1 不做（A14，客户端防重）；e2e 默认连共享 panji 库并每次重置 schema（建议后续改独立库——QA 复跑时已用专用库隔离共享库并发，正式化需另派单动 test/ 基建）。

## 范围与红线

- 本轮零代码修改（预置实现经实读确认合规）；无新接口、无 M1 外功能。

## 最终结论

**PASS**

## 是否允许 M1-005 置 DONE

**YES** —— 建议总控将 M1-005 置 DONE。至此 M1 后端主链接口（M1-002~M1-005）全部收口，iOS M1-009/M1-010 的前置就绪。

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M1-005 独立复核（3.11/3.12 逐项 + 冒烟 8/8 + e2e 29/29）→ PASS |
