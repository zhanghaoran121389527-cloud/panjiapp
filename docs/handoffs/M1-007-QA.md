# M1-007 QA 审核交接单（QA）

- 审核对象：M1-007【iOS】收藏柜
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-25（同日）
- 审核依据：API_CONTRACT v2.3 §3.4/3.5、DESIGN_SYSTEM v1（§2/§6.2/§6.3/§6.7）、docs/design/03-cabinet.md 与 08-states.md、验收清单 v2.1、TASKS M1-007 验收条件
- 编号映射说明：任务书 QA 要求中的「B07 无封面占位、B18 隔离冒烟」对应现行清单 v2.1 的 **B12（无封面创建，卡片占位）** 与 **B19（用户隔离）**（B07=API500、B18=登录状态失效 已在 M1-003/M1-006 覆盖），本单按此映射执行。
- 审核方式：实读全部实现文件（3 新 + 4 触点）+ 云端 CI 构建证据 + 截图像素核验

## 契约与规格核对

| 项 | 实现（实读） | 结果 |
|---|---|---|
| §3.4/§3.5 DTO | CategoryDTO/CategoriesResponse/ItemDTO/ItemsResponse，camelCase 与契约一致（ItemDTO: id/name/coverImageUrl/categoryId/dayCount/createdAt） | PASS |
| 品类分组 | CabinetStore.sections：compactMap 仅含有玩物的品类，顺序=服务端 sort_order；组内 createdAt 倒序（服务端序） | PASS |
| 卡片三要素 | 封面 1:1 + 名称（heading，1 行截断）+ 盘玩天数（caption 三态：尚未盘玩/盘玩 1 天/盘玩 N 天，直接映射服务端 dayCount，客户端不算） | PASS |
| 无封面占位（“B07”） | coverImageUrl=null 或加载中/失败 → `accentSoft` 底 + **玩物名首字**（heading/textSecondary），静默降级不重试 —— 与 DESIGN_SYSTEM §6.3、08-states C 一致 | PASS |
| 空态文案 | tray 圆底图标 +「还没有玩物」+「创建第一件玩物，开始记录它的变化」+ CTA（宽 240）；右上「+」与 CTA 同行为（线框 03 明确不隐藏「+」） | PASS |
| 下拉刷新 | refreshable；成功替换数据；失败保留内容仅顶部 dangerSoft 横幅「刷新失败，下拉重试」3s 自动消失 | PASS |
| 首载骨架/整页错误态 | 4 个 surface 色块呼吸动画；失败态 wifi.slash（网络）/三角形（服务器）分文案 + 重试（08-states A） | PASS |
| 401 处理 | load/refresh 捕获 authRequired → session.signOut() 回登录 | PASS |
| 图片 URL | 经 `APIClient.imageURL(for:)` 解析相对路径（裁决 A3），页面不自拼 URL | PASS |
| 并发拉取 | CabinetStore `async let` 并发 /categories + /items | PASS |
| token 注入 | SessionStore.token 由 private 改 internal（只读；写入仍仅 login/signOut）；请求注入 Bearer | PASS |

## B19 用户隔离冒烟（DEBUG「开发：切换账号」入口）—— 静态级 PASS

- 入口实现：CabinetView toolbar `#if DEBUG` 包裹 `Button("开发：切换账号") → session.signOut()`，**Release 构建不编译该代码**（编译级排除，验收⑥）✓。
- 隔离语义链路：signOut → phase=needLogin → 登录另一账号 → CabinetStore 以新 token 拉 /categories+/items；服务端按 user_id 隔离（M1-003 已实测 B 列表仅自己数据、越权 404）。两端语义闭环，静态链路 PASS。
- 交互级切号冒烟（模拟器内点按两账号切换）本机无模拟器 → **NOT VERIFIED（交互级）**，并入 M1-012 真机验收。

## 构建证据（云端 CI）

- 18:56 CI 运行（M1-007 代码，14 个 Swift 文件）：**`** BUILD SUCCEEDED **`**（Xcode 16.4 / iPhoneSimulator SDK 18.5）+ 模拟器 boot/install/**launch（pid 8874）** + 截图回传 + `STEP4_ALL_OK` ✓。
- 新截图已回传本地并完成像素核验（1206×2622）：暖米白 90.7% + 木棕 1.5% + 深茶褐 0.3% + **surface 输入框色 4.9%**（中下部采样=surface）——为**登录页**特征（未登录启动即 LoginView），App 正常启动无崩溃 ✓。
- 交互级 UI 走查（分组陈列/占位首字/切号点按）无模拟器环境，NOT VERIFIED（交互级），并入 M1-012。

## 工程与范围核对

- pbxproj：占位视图移除、Features/Items 分组与 3 文件注册（Sources 14 个 Swift 双向一致——与交接单静态自检一致，CI 编译佐证）✓。
- 越界申报：Auth/Network 4 处机械触点（AuthRootView 接线、token 可见性、DTO、imageURL）为替换占位视图的机械必需，已在交接单 §2 申报；总控初审接受。无其他越界。
- 无 M1 外功能：卡片点击进详情（M1-009）、真实创建页（M1-008）均未实现，占位 sheet 已注明待替换；无社区/商城痕迹。

## 发现的问题

- 无阻断问题。
- 记录项（非阻断）：① 交互级冒烟（B12 占位视觉、B19 切号点按、刷新横幅动效）NOT VERIFIED，并入 M1-012；② 「+」与 CTA 目前开临时占位 sheet（M1-008 替换，已注明）；③ 卡片点击无导航（M1-009 接线，任务书有意为之）。

## 最终结论

**PASS**（附 NOT VERIFIED：交互级走查并入 M1-012；CI 构建+launch 证据已齐）

## 是否允许 M1-007 置 DONE

**YES** —— 建议总控将 M1-007 置 DONE；其后 M1-008（创建玩物）/M1-009（详情+时间轴）前置逐步满足。

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M1-007 独立复核（契约/卡片三要素/占位/空态/切号 #if DEBUG + CI 绿 + 截图像素核验）→ PASS |
