# M1-006 QA 审核交接单（QA）

- 审核对象：M1-006【iOS】网络层 + 登录/昵称流程
- 审核角色：QA（独立否决权）
- 审核时间：2026-08-25（同日）
- 审核依据：API_CONTRACT v2.3 §3.1~3.3、DESIGN_SYSTEM v1、docs/design/08-states.md、验收清单 v2.1 B01~B05、TASKS M1-006 验收条件
- 审核方式：实读全部 8 个新增文件 + PanJiTheme/RootView/pbxproj + 云端 CI 构建证据核对 + B01~B05 静态映射冒烟

## 契约核对（3.1~3.3 使用）

| 项 | 实现 | 结果 |
|---|---|---|
| POST /v1/auth/dev-login | APIClient 根地址拼接 `/v1/`，`/auth/dev-login`，body `{phone}`；响应 DevLoginResponse(token/user/isNewUser) camelCase 对齐 | PASS |
| GET /v1/me 启动恢复 | SessionStore.restore() → `/me` 带 Bearer；401 → signOut 回登录；网络失败 → restoreFailed 重试态 | PASS |
| PATCH /v1/me 昵称 | saveNickname → `/me` PATCH `{nickname}`；1~20 字客户端预校验（超长硬截断、trim 空禁用） | PASS |
| 错误结构 `{code,message}` | serverError 解析 code/message → APIError；401/AUTH_REQUIRED 全局回登录不显示文案（08-states B 表） | PASS |
| Bearer token 注入 / 10s 超时 | APIClient setValue Bearer；timeoutIntervalForRequest/Resource=10s | PASS |
| token 存 UserDefaults（A7） | SessionStore session.token 三键持久化 | PASS |

## 18 色 token vs DESIGN_SYSTEM v1 —— 18/18 一致（逐值核对）

`background F6F1E7 / surface FFFDF8 / surfacePressed F2EBDD / textPrimary 4A3728 / textSecondary 8A7B6D / textTertiary B0A28F / accent 8C6D4F / accentPressed 79593F / accentSoft EFE5D3 / onAccent FFFDF8 / divider E9E0CE / disabledFill EFE9DB / disabledText C7BBA9 / danger B1502F / dangerPressed 98422A / dangerSoft F7E8E1 / success 6E7F5B / scrim 黑40%` —— 与 DESIGN_SYSTEM v1 §2 全表一致；7 级字号、间距/圆角、卡片阴影、主按钮（§6.1 高 52/radiusM/accent）、输入框（§6.4 高 48/聚焦 accent 1.5pt）均按 v1 实现；v0 暂定文案已清除。PASS。

## B01~B05 冒烟（静态映射 + CI 构建证据；交互级标注见下）

| 编号 | 场景 | 代码路径与判定 |
|---|---|---|
| B01 | 无网络 | 有 token：restore() 网络失败 → RestoreFailedView（wifi.slash + 标题/说明/重试，08-states A）不崩溃；无 token：直接 needLogin 本地可显示。登录提交断网 → 行内网络文案。PASS（静态） |
| B02 | 操作中断网表单保留 | submit 失败仅置 errorText，phone/nickname 状态保留，defer 恢复按钮可重试。PASS（静态） |
| B03 | API 超时 | URLSession 10s 超时 → networkUnreachable → 「网络不太顺畅，请检查后重试」；提交中态经 defer 复位，不无限转圈。PASS（静态） |
| B04 | API 400 | VALIDATION_ERROR → validation(message) 显示后端中文原文（行内 danger caption，主按钮上方）。PASS（静态） |
| B05 | API 401 | authRequired → signOut 清 token → needLogin，不显示文案（08-states B 表）。PASS（静态） |

说明：
- **B12**：清单 v2.1 中 B12 =「无封面创建」，属 M1-008 范围，M1-006 不适用；断网/超时已按 B02/B03 覆盖（M1-006 交接单 §16 对 B12 的提法系编号笔误）。
- **交互级 NOT VERIFIED**：本机无 iOS 模拟器，B01~B05 的真机/模拟器交互走查无法本机执行；已由云端 CI 提供「构建成功 + 模拟器安装 + launch（pid 7543）+ 截图」运行级证据。**交互冒烟并入 M1-012 真机验收执行。**

## 登录页回填上次手机号 —— 预期行为，非缺陷

`SessionStore.lastPhone`（UserDefaults `session.lastPhone`，登录成功时写入；signOut 保留）→ `LoginView.onAppear` 回填。与线框 01 默认状态及总控口径一致。确认非缺陷。

## 构建证据（云端 CI）

- 18:48 CI 运行（M1-006 代码）：`** BUILD SUCCEEDED **`（Xcode 16.4 / iPhoneSimulator SDK 18.5）；产物 Info.plist 实测 `APIBaseURL => http://localhost:3000`、`CFBundleDisplayName=盘迹`、MinimumOSVersion 17.0、UIDeviceFamily [1]、仅竖屏、ATS 局域网例外 —— 全部正确。
- 模拟器 boot + install + launch（pid 7543）+ screenshot + `STEP4_ALL_OK`。
- 注意：该运行的新截图未回传本地（ci-logs/m0-003-simulator.png 仍为 17:55 M0-003 版本地副本）；launch 证据以 18:48 step4.log 为准，如总控需像素核验可回传新图。

## 工程与范围核对

- pbxproj 注册 8 新文件（BuildFile/FileReference/Group/Sources 四段闭合）+ Features/Auth 分组 ✓；RootView → AuthRootView 接管 ✓。
- 越界申报：RootView 与 pbxproj 属任务书「允许修改范围」之外，iOS 已在交接单 §2 申报为机械必需，总控 v3.17 已裁定接受。无其他越界。
- 无硬编码业务 API（AppConfig xcconfig 注入 + 本地 fallback 仅开发默认）；无 secret 进入 App；无 M1 外功能（B8 切号入口未实现，归 M1-007，总控已裁决）。

## 发现的问题

- 无阻断问题。
- 记录项（非阻断）：① 线框 01 与 08-states 两处微不一致（登录失败文案、行内错误位置），实现按 08-states 全局规范执行，待总控/UI 决定是否回改线框；② 新截图未回传本地（见上）；③ e2e 套件 describe 标题仍写「契约 v2.2」（后端文案，随缘修正）；④ 交互级 B01~B05 未本机实跑（并入 M1-012）。

## 最终结论

**PASS**（附 NOT VERIFIED 项：交互级冒烟并入 M1-012 真机验收；CI 构建+launch 证据已齐）

## 是否允许 M1-006 置 DONE

**YES** —— 建议总控将 M1-006 置 DONE；其后 M1-007（收藏柜，前置 M1-003+M1-006 均已 DONE）可派发。

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M1-006 独立复核（契约 3.1~3.3 + 18 token + B01~B05 静态映射 + CI 证据）→ PASS |
