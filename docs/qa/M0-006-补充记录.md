# M0-006 补充记录（B12 修订口径续做，非 macOS 段）

- 任务：M0-006 非 macOS 段（总控指令：B12 口径续做）
- 角色：QA
- 时间：2026-08-25（同日）
- 范围声明：本轮未修改任何代码、契约、看板状态；仅产出本记录（docs/qa/**）。**不宣布 M0-006 PASS、不宣布 M0 DONE。**
- 前置阅读：AGENTS.md、TASKS.md（M0-006/M0-001 任务书、B12）、docs/handoffs/M0-006.md、infra/README.md、apps/api/scripts/dev-db.mjs —— 均已实读。

---

## 1. M0-001 复核（B12 口径：本机真实 PostgreSQL 16 路线，Docker 实跑豁免）

### ① 一条命令启动 —— PASS

命令（工作目录 `apps/api`）：

```powershell
$env:DB_PORT='5433'; node scripts/dev-db.mjs start
```

实际输出：

```
waiting for server to start.................................... done
server started
database panji already exists, skip create
PostgreSQL ready: postgresql://panji:panji_dev@localhost:5433/panji
停止：npm run db:stop
```

结论：一条命令启动成功。端口 5433 为本机实际端口（5432 被本机预装 PG 占用），符合 B12"或实际端口"口径；数据目录 `D:\panji-data`、二进制 `D:\panji-pg-bin`（PG16）。
过程注记：首次执行被工作区沙箱拦截（pg_ctl 拉起 postgres 子进程 popen 拒绝），已按流程提权重试同一命令后成功（两次提权：start 与 stop 信号），行为与命令未变。

### ② 可连接 panji 库 —— PASS

命令（临时脚本走 pg 驱动，用后即删，未落仓库）：

```javascript
new Client({connectionString:"postgresql://panji:panji_dev@localhost:5433/panji"})
  → SELECT version(), current_database(), current_user
```

实际输出：

```
CONNECT OK: panji | panji | PostgreSQL 16.14, compiled by Visual C++ build 1944, 64-bit
```

结论：账号 panji / 密码 panji_dev / 库 panji / PostgreSQL 16.14，全部与 ARCHITECTURE §6 与 B12 口径一致。

### ③ 停止再启动后数据仍在 —— PASS

步骤与输出：

| 步骤 | 命令 | 输出 |
|---|---|---|
| 写入测试数据 | 专用库 `panji_qa_b12` 建表 `_qa_b12` 插入 `(1,'b12-persistence-probe')` | `TEST DATA WRITTEN: rows=1` |
| 停止服务 | `node scripts/dev-db.mjs stop` | `waiting for server to shut down.... done` / `server stopped` |
| 停止确认 | `Test-NetConnection 127.0.0.1:5433` | `False`（无监听） |
| 重新一条命令启动 | `$env:DB_PORT='5433'; node scripts/dev-db.mjs start` | `server started` / `PostgreSQL ready: ...` |
| 重启后验证 | 重连 `panji_qa_b12` 查 `_qa_b12` | `AFTER RESTART: [{"id":1,"note":"b12-persistence-probe"}]` |
| 清理 | `DROP DATABASE panji_qa_b12 WITH (FORCE)` | `QA DB CLEANED` |

结论：跨停启数据保留（本机持久化目录 `D:\panji-data`，等价于 compose 的 named volume 语义）。M0-001 验收①②③ 在 B12 口径下全部实跑通过。

### compose 静态合规核对（Docker 实跑豁免，记已知缺口）

- 静态核对 8 项：镜像 `postgres:16-alpine` / container_name / 账号 panji+panji_dev / 端口映射 / named volume `panji_pgdata` / healthcheck pg_isready / 无任何建表 SQL（A1）/ 连接串与 .env.example 一致 —— **全部 PASS**（与 M0-001-QA.md 早前逐行核对一致；文件现 22 行）。
- compose 头部注释已由 DBI 更新为"备选路线（裁决 B12）"✓。
- **发现（非阻断，DBI 待办）**：`infra/README.md` 尚未同步 B12 口径——仍以 Docker 为主路线叙述，未提及本地 PG16 路线（dev-db.mjs），与 compose 新注释互相脱节。QA 修改范围为 docs/qa/**，无权代改，请总控转 DBI 补同步。
- Docker 实跑：**豁免（B12）**，记为已知缺口，待未来有 Docker 机器时按 infra/README 补验（启动/psql/容器重建持久化）。

## 2. npm run lint 复核（M0-007 复测）—— PASS

- `npm run lint`（apps/api）：eslint 调用完整执行、**无任何 lint 报错输出**；本 harness 中 npm.ps1 尾部的 `$LASTEXITCODE` 自检报兼容噪音（不影响 eslint 本身结果，已注明）。
- node 等价直跑（与 package.json lint 脚本完全一致的 glob）：`node node_modules/eslint/bin/eslint.js "src/**/*.ts" "test/**/*.ts" "scripts/**/*.mjs"` → **exit 0**。
- 结论：M0-007 的 lint 修复在当前树上持续有效；M0-006 的 lint 阻塞项维持解除状态。

## 3. 云端 CI（M0-006A）证据复核 —— 本轮跳过

- 按总控指令：当前 CI 运行仍红（TASKS v3.12：GitHub Actions `ios-build` 未绿），本段跳过。
- 记录：**待 CI 绿后复核**（届时核对构建日志、Info.plist 注入核对、模拟器启动/截图工件），CI 绿 + 工件复核通过前，M0-006A 不得 DONE、M0-006 不得 PASS。

## 4. 本轮结论

| 项 | 结论 |
|---|---|
| M0-001 ①启动 ②连接 ③停启持久化（B12 本地 PG16 路线） | **全部 PASS**（真实 PostgreSQL 16.14） |
| compose 静态合规 + Docker 豁免缺口 | PASS + 已知缺口（记录在案） |
| npm run lint 复测（M0-007） | **PASS**（exit 0） |
| CI（M0-006A）证据 | 跳过，待 CI 绿后复核 |

遗留清单：① `infra/README.md` B12 口径同步（DBI）；② Docker 实跑豁免缺口（未来有机器补验）；③ CI 红 → 待绿；④ M0-003 构建证据随 CI 复核回填。

**M0-006 整体状态维持 PARTIAL PASS；不宣布 M0 PASS。** 本轮到此停止，等总控审核与下一步指令。

## 5. 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：B12 口径 M0-001 复核（①启动/②连接/③持久化 全 PASS）+ lint 复测 PASS + CI 跳过记录 |
