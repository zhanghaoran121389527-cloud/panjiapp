# M0 集成验收报告（M0-006 收尾）

- 报告编号：M0-验收报告（QA）
- 任务：M0-006 M0 集成验收（非 macOS 段 + macOS 证据段收尾）
- 角色：QA
- 时间：2026-08-25（同日）
- 依据：TASKS M0-006 验收条件（B12/B13 修订口径）、docs/qa/验收清单.md v2.1、各任务交接单与 QA 复审单
- 说明：本报告为 M0-006 五项验收的逐项结论汇总；**M0 PASS 的正式宣布权属总控**。

## 五项验收逐项结论

### ① DB 实跑验证 —— PASS（B12 本机 PG16 路线）

- M0-001 验收①②③ 以本机真实 PostgreSQL 16.14 实测（docs/qa/M0-006-补充记录.md）：
  - ① 一条命令启动：`node scripts/dev-db.mjs start` → `server started`、ready 连接串 `postgresql://panji:panji_dev@localhost:5433/panji` ✓
  - ② 可连接 panji 库：`CONNECT OK: panji | panji | PostgreSQL 16.14` ✓
  - ③ 停启后数据仍在：写入探针行 → stop（5433 无监听）→ start → `AFTER RESTART: [{"id":1,"note":"b12-persistence-probe"}]` ✓
- compose 静态合规 8 项 PASS（镜像/账号/端口/named volume/healthcheck/无建表 SQL/连接串/持久化命令）；头部已标注 B12"备选路线"；`infra/README.md` 已同步 B12 口径（标准=本地 PG16 路线，compose 备选）。
- Docker 实跑 = **豁免（B12）**，记入已知缺口（团队无 Docker 机器，有机器时补验）。
- 数据层规格另经 M1-001 QA 独立库全量验证 ALL PASS（docs/handoffs/M1-001-QA.md）。

### ② API health 通过 —— PASS

- 现场实测（docs/handoffs/M0-006.md）：真实进程启动、13 条路由全部挂载 `/v1/`；`GET /v1/health` → 200 `{"status":"ok"}`；未知路由 404 `{"code":"NOT_FOUND","message":"资源不存在"}`（body 全等）；无 token/坏 token 401 `AUTH_REQUIRED`；错误体无 stack/secret/路径泄漏。
- e2e 底座套件（m0-002.e2e-spec）4/4 通过（历次复跑持续绿）。

### ③ iOS 构建证据确认 —— PASS（B13 云端 CI）

- 云端 CI（GitHub Actions run #32833177820，pipefail 生效）**BUILD SUCCEEDED**（Xcode 16.4，iPhone 模拟器目标）。
- 产物 Info.plist 实测：`APIBaseURL => http://localhost:3000`（xcconfig 注入链真实生效）、`CFBundleDisplayName=盘迹`、iOS 17.0、仅竖屏、ATS NSAllowsLocalNetworking —— 与 M0-003 验收③/B2/B3 一致。
- 模拟器安装并 launch 成功（pid 7543，无启动崩溃），截图回传 `ci-logs/m0-003-simulator.png`。
- QA 截图核验（本机程序化像素级）：PNG 合法（1206×2622，111KB）；采样 198112 点：暖米白背景 #F6F1E7 占 96.3%、木棕圆标 #8C6D4F 占 1.5%、深茶褐文字 #4A3728 占 0.1%，中心/角点均为暖米白 —— 与 RootView 占位页（暖米白底 + 木棕"盘"圆标 + 深茶褐"盘迹"标题 + 底部 API 地址文本）结构完全吻合，非崩溃黑屏/白屏。
- **如实说明**：QA 模型无图像输入能力，未做人眼级逐字目视；文字级最终目视请总控在 GitHub 页面/工件侧确认（截图、构建日志、Info.plist 核对日志均可复查）。像素级证据 + 日志证据链已构成验收②证据。
- 注：`ci-logs/` 内两份 .log 为早期失败轮次残留（iPhone 15 模拟器缺失错误），不影响绿跑证据（绿跑证据以 TASKS v3.13 与 run #32833177820 记录为准）。

### ④ DESIGN_SYSTEM v1 与验收清单 v2 就绪 —— PASS

- M0-004 DONE：DESIGN_SYSTEM v1 定稿（18 色 token + 字号/间距/圆角/组件规范）+ docs/design/ 9 线框（含 20 步覆盖矩阵）。
- M0-005 DONE：docs/qa/验收清单.md v2.1（20 步主链 A01~A20 + 异常边界 21 项 + 持久化专项 5 项 + 编辑/删除冒烟 7 项 + 契约核对表对齐 API_CONTRACT v2.3）。

### ⑤ 问题清零或已裁决 —— PASS

| 历史问题 | 处置 | 状态 |
|---|---|---|
| lint exit 2（scripts glob 失效） | M0-007 修复 → DONE；QA 复测 `npm run lint` 无报错、node 等价 exit 0（M0-006-补充记录） | 关闭 |
| e2e DELETE 红（旧硬删除断言） | M1-003 改为逻辑删除实现与断言 → QA 独立复跑 e2e 29/29 全绿 + 冒烟 ALL PASS（docs/handoffs/M1-003-QA.md） | 关闭 |
| M0-001 Docker 实跑缺环境 | B12 裁定：本机 PG16 路线替代（实测 PASS），Docker 豁免记已知缺口 | 已裁决 |
| M0-003 无构建证据 | B13 云端 CI 真绿 + 截图回传，验收①②③ 证据齐备 | 关闭 |
| infra/README 契约引用过期 / B12 未同步 | DBI 整改完成（当前 README 已为 B12 口径） | 关闭 |
| e2e 共享库并发竞争（QA 复跑时发现的环境问题） | 专用库隔离后 29/29 全绿；建议 e2e 用独立库（非代码缺陷，已记录） | 已裁决 |

## 遗留与已知缺口（均非阻断）

1. Docker 实跑豁免（B12）——有 Docker 机器时补验 compose 三命令。
2. 截图像素级核验通过、人工逐字目视待总控在 GitHub 侧最终确认。
3. e2e 共享库并发风险——建议后续 e2e 固定使用独立库（如 CI 每个 job 独立库）。

## 总体结论

**M0-006 五项验收全部满足（①PASS ②PASS ③PASS ④PASS ⑤PASS），PARTIAL 遗留项全部核销。M0 具备放行条件——是否宣布 M0 PASS 与放行 M1，由总控决定。**

## 变更记录

| 时间 | 变更 |
|---|---|
| 同日 | 建立：M0-006 五项验收收尾汇总；M1-003 QA 复核 PASS（e2e 29/29 + 冒烟 ALL PASS）；截图像素级核验完成 |
