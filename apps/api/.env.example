# 复制为 .env 后按需修改；不建 .env 时使用默认值（连本地 panji 库）
PORT=3000
JWT_SECRET=panji-m1-dev-secret
# 与 infra/.env.example 一致（DBI T-001）
DATABASE_URL=postgresql://panji:panji_dev@localhost:5432/panji
UPLOAD_DIR=uploads
