// 盘迹 iOS 构建配置（T-003 工程底座）
// 注意：xcconfig 中以 // 开头的是注释，URL 中的 // 需写成 /$()/。

// M1 开发期 API 根地址（不含路径，业务前缀 /v1/ 由网络层在 T-106 拼接；静态图片 /uploads/* 同根拼接）
//   模拟器：http://localhost:3000
//   真机联调：改成开发机局域网 IP，例如 http://192.168.1.100:3000
API_BASE_URL = http:/$()/localhost:3000
